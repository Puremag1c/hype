# Dependency compatibility matrix for HYPE
# Format: name|cmd|version_extract|min_version|max_version|brew_pkg|required
# - max_version empty = no upper limit
# - min_version empty = presence check only
# - required=false = warn if missing, don't block
beads|bd|bd version|0.55.0|0.56.99|beads|true
gh|gh|gh --version|2.0.0||gh|true
jq|jq|jq --version|1.6||jq|true
gitleaks|gitleaks|gitleaks version|||gitleaks|false
claude|claude|claude --version|||claude|true
